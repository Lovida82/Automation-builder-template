# 빠른 시작 안내

업무 자동화 프로그램을 **대화만으로** 만들어주는 도구를 처음 사용하시는 분들을 위한 안내입니다.

---

## 본인은 어느 그룹인가요? (먼저 확인하세요)

| 그룹 | 추천 경로 | 예상 소요 시간 |
|------|---------|--------------|
| **A. AI 도구 처음** (Claude Code/CLI 들어본 적도 없음) | 섹션 1 → 2 → 3 → 4 → 5 | 처음 셋업 30~60분 |
| **B. ChatGPT/Claude 웹은 써봤음** (CLI는 처음) | 섹션 1 → 3 → 4 → 5 | 처음 셋업 20~40분 |
| **C. CLI 도구 사용 경험 있음** (Claude Code 또는 다른 도구) | 섹션 4 → 5 만 보면 됨 | 5~10분 |

> 막혀도 괜찮습니다. 한 번만 셋업해두면 다음부터는 5분 안에 새 자동화를 시작할 수 있어요.

---

## 1. 큰 그림 (이게 뭔가요?)

### 무엇을 할 수 있나요?

매일 반복하는 웹/SAP/엑셀 업무를 **AI와 대화만 하면서** Windows 실행파일(.exe)로 자동화할 수 있습니다.

**예시:**
- "매일 아침 SAP 들어가서 전일 판매실적 다운로드받고 정리하는 일" → AI랑 대화하면서 자동화 만들기 → 다음 날부터는 .exe 더블클릭 한 번이면 끝
- "사내 포털에서 매주 보고서 양식 다운로드 → Excel로 가공"
- "여러 거래처 사이트 돌면서 가격 정보 수집"

### 어떻게 작동하나요?

```
[당신]                          [AI]
   │                              │
   ├─ 업무 설명                    │
   │                              ├─ 질문하고
   ├─ 화면 캡쳐 폴더에 넣기         │   환경 셋업 자동
   │                              │
   ├─ AI 질문에 답변              ├─ 코드 작성 자동
   │                              ├─ 테스트 실행 자동
   ├─ "맞아요" / "다시 해줘"        │
   │                              ├─ 에러 분석 자동
   └─ 결과 받기                   └─ .exe 빌드 자동
```

**당신이 할 일은 4가지뿐:**
1. 캡쳐 넣기
2. 질문에 답하기
3. 결과 확인하기
4. 자연어로 명령하기 ("다음", "다시", "이거 바꿔줘")

명령어 입력, 코드 수정, 설치, 빌드는 **AI가 다 합니다.**

---

## 2. 필요한 도구 설치 (그룹 A: 처음이신 분)

### 2-1. Node.js 설치 (Claude Code 사용 시 필요)

Claude Code는 Node.js 위에서 돌아갑니다. 컴퓨터에 한 번만 설치하면 됩니다.

**설치 방법:**
1. https://nodejs.org/ko 접속
2. 좌측의 **LTS** 버전 다운로드 (현재 v22 이상)
3. 다운받은 파일 더블클릭 → "Next" 계속 누르기 → 설치 완료
4. 설치 확인: 시작 메뉴에서 "cmd" 또는 "PowerShell" 검색해서 열고, 아래 명령어 입력
   ```
   node --version
   ```
   `v22.x.x` 같이 버전이 나오면 성공

**막힘 사례:**
- `node`가 명령어로 인식 안 됨 → 설치 후 cmd/PowerShell을 **다시 열어야** 합니다 (열려있던 창은 인식 못 함)
- 회사 PC에서 설치 권한 없음 → 사내 IT팀에 "Node.js LTS 설치 요청" 하시면 됩니다

### 2-2. Python 설치 (자동화 프로그램이 Python으로 만들어지기 때문)

1. https://www.python.org/downloads/ 접속
2. **Python 3.12** 또는 **3.13** 다운로드 (Windows 64-bit)
3. **[매우 중요]** 설치 마법사 첫 화면에서 **"Add Python to PATH"** 체크박스를 반드시 켜기
4. "Install Now" 클릭
5. 확인: 새 cmd 창에서
   ```
   python --version
   ```
   `Python 3.12.x` 식으로 나오면 성공

**막힘 사례:**
- `python`이 인식 안 됨 → PATH 체크 안 했을 가능성. 제어판에서 Python 제거 → 재설치 시 PATH 체크
- 회사 PC 권한 부족 → "사용자만 설치" 옵션으로 진행 또는 IT팀 요청

### 2-3. Git 설치 (선택, 권장)

코드 버전관리 및 일부 도구 동작에 필요할 수 있습니다.

1. https://git-scm.com/download/win 에서 다운로드
2. 기본 설정으로 설치
3. 확인: `git --version`

---

## 3. CLI AI 도구 선택 & 설치

이 폴더는 **여러 CLI AI 도구**와 호환됩니다. 본인 환경에 맞는 것을 선택하세요.

### 3-1. 도구별 비교

| 도구 | 모델 | 장점 | 단점 | 비용 |
|------|------|------|------|------|
| **Claude Code** | Claude (Anthropic) | 본 PRD가 가장 잘 맞음. 코딩 품질 최상위 | 별도 구독 필요 | Pro 월 $20~ / API 종량제 |
| **Codex CLI** | GPT (OpenAI) | ChatGPT 구독자에게 친숙 | 일부 Tool 동작 차이 | ChatGPT Plus 포함 / API 별도 |
| **Gemini CLI** | Gemini (Google) | 무료 티어 관대 | 한국어 코딩 컨텍스트 약간 약함 | 무료 티어 + 유료 |

> **추천**: 본 PRD는 Claude Code 기준으로 작성되었습니다. 다른 도구도 동작하지만, **Claude Code가 가장 안정적**입니다.

### 3-2. Claude Code 설치 (권장)

```
npm install -g @anthropic-ai/claude-code
```

위 명령어를 cmd 또는 PowerShell에 입력 후 엔터.

**첫 실행 & 로그인:**
1. 작업할 폴더를 만들고 (예: `C:\AutoProjects\sap-sales`) 그 폴더로 이동
   - 탐색기에서 폴더 만들기
   - 그 폴더 빈 공간에서 **Shift + 우클릭** → "여기에 PowerShell 창 열기" 또는 "여기서 터미널 열기"
2. 터미널에서:
   ```
   claude
   ```
3. 처음 실행 시 브라우저가 열리며 Anthropic 로그인 화면이 나옵니다 (Claude.ai 계정 또는 API 키)
4. 로그인 완료 후 터미널에 채팅 UI가 나오면 준비 완료

**구독 종류:**
- **Claude Pro/Max** 구독자: 그대로 로그인하면 사용 가능 (월정액)
- **API 키 사용자**: Anthropic Console에서 API 키 발급받아 `setx ANTHROPIC_API_KEY "키값"` 으로 등록 (사용량 종량제)

### 3-3. Codex CLI 설치 (대안)

```
npm install -g @openai/codex
```

실행: `codex`
첫 실행 시 ChatGPT 계정 로그인 또는 OpenAI API 키 입력

### 3-4. Gemini CLI 설치 (대안)

```
npm install -g @google/gemini-cli
```

실행: `gemini`
첫 실행 시 Google 계정 로그인 또는 API 키 입력

---

## 4. 프로젝트 시작하기 (모든 그룹 공통)

### 4-1. 작업 폴더 만들기

자동화 하나당 **별도 폴더** 한 개를 사용합니다. 예시:

```
C:\AutoProjects\
├── sap-sales\              ← SAP 판매실적 자동화
├── portal-report-download\ ← 사내 포털 보고서 자동화
└── price-monitoring\       ← 거래처 가격 모니터링
```

폴더명은 영문 + 하이픈 권장 (한글/공백/특수문자 가급적 피하기 — 일부 도구에서 문제 가능).

### 4-2. PRD 파일 넣기

방금 만든 폴더 안에 **`AUTOMATION_BUILDER_PRD.md`** 파일을 복사해 넣으세요.

이 파일이 AI에게 "어떻게 일해야 하는지" 알려주는 지침서입니다.

### 4-3. CLI AI 도구 켜기

폴더 안에서 터미널 여는 방법 (셋 중 하나):

**방법 1 (가장 쉬움):** 폴더 빈 공간에서 **Shift + 우클릭** → "여기에 PowerShell 창 열기"

**방법 2:** 폴더 주소창에 `cmd` 입력 후 엔터

**방법 3:** cmd 또는 PowerShell 열고 `cd "C:\AutoProjects\sap-sales"` 입력

터미널이 열리면:
```
claude
```
(또는 사용하는 도구의 명령어: `codex`, `gemini` 등)

### 4-4. 첫 명령 (이게 마법의 한 줄)

채팅창이 열리면 아래 한 줄만 입력하고 엔터:

```
AUTOMATION_BUILDER_PRD.md 파일을 읽고 그대로 시작해줘
```

이제부터는 AI가 알아서 안내합니다. 당신은 답변만 하면 됩니다.

---

## 5. 진행 중 자주 마주치는 상황

### 5-1. "이거 처음인데 잘 모르겠어요"

→ AI에게 그대로 알려주세요. "처음이라 잘 모르겠어요" / "이거 어떻게 하는 거예요?" — AI가 친절하게 풀어줍니다.

### 5-2. 캡쳐 어떻게 떠요?

**Windows 기본:**
- `Windows키 + Shift + S` → 영역 선택 → 자동으로 클립보드에 복사됨
- 캡쳐 도구 앱(Snipping Tool)에서 직접 저장 가능

**파일로 저장:**
- 캡쳐 후 그림판 또는 캡쳐 도구에서 PNG로 저장
- 또는 캡쳐 직후 뜨는 알림 클릭 → 편집창에서 저장

**저장 위치:**
- AI가 알려준 폴더(보통 `프로젝트폴더\captures\full_flow\`)에 저장
- 파일명: `01_로그인화면.png`, `02_메뉴.png` ... 순서대로

### 5-3. 캡쳐 폴더가 안 보여요

→ AI가 폴더를 아직 안 만들었을 수 있습니다. "캡쳐 폴더 만들어줘"라고 하시면 됩니다.

### 5-4. AI가 "준비됐어요" 같은 답변을 기다리는 것 같아요

→ 그냥 자연어로 답하시면 됩니다. "네 준비됐어요" / "실행해주세요" / "맞아요" / "다음" — 다 알아듣습니다.

### 5-5. 에러가 났어요

→ "에러 났어요" 한 마디면 됩니다. AI가 직접 로그를 확인하고 해결합니다.
이게 막혀 있는 게 아니라, **AI한테 "막혔어"라고 알려주는 게 다음 단계입니다.**

### 5-6. 어디까지 했는지 모르겠어요

→ 폴더 안 **`PROGRESS.md`** 파일을 열어보면 현재 상태가 다 적혀 있습니다.
→ 또는 AI에게 "지금까지 어디까지 했어?" 물어보면 요약해줍니다.

### 5-7. 다른 컴퓨터에서 이어서 할 수 있나요?

→ 폴더 전체를 USB나 OneDrive로 복사해서 다른 컴퓨터에서 CLI AI를 켜면 됩니다. AI가 자동으로 진행상황을 이어받습니다.
→ 단, Node.js / Python / CLI 도구는 그 컴퓨터에도 설치되어 있어야 합니다.

### 5-8. 세션을 끄고 나중에 이어서 할 수 있나요?

→ 네. 터미널을 닫아도 폴더의 상태 파일(`PROGRESS.md`, `DECISIONS.md`, `STEP_LOGS/`)에 모든 게 저장되어 있습니다.
→ 나중에 같은 폴더에서 CLI AI를 다시 켜고 "이어서 진행해줘" 하시면 됩니다.

### 5-9. 잘못 만들어서 다시 처음부터 하고 싶어요

→ 폴더를 통째로 삭제하고 새 폴더에서 다시 시작하시면 됩니다. 부담 없이 다시 하세요.
→ 또는 AI에게 "처음부터 다시 하자"고 하시면 진행 상태를 초기화해줍니다.

### 5-10. AI 사용량/요금이 걱정돼요

| 도구 | 대략적 비용 |
|------|------------|
| Claude Pro | 월 $20, 일정 사용량 제한 있음. 자동화 1개 만드는 데 충분 |
| Claude API | 자동화 1개당 보통 $1~5 (복잡도에 따라) |
| ChatGPT Plus + Codex | 월 $20, 사용량 제한 있음 |
| Gemini CLI 무료 티어 | 일정 사용량까지 무료 |

처음 시작은 **무료 티어(Gemini)** 또는 **월정액 구독**으로 충분합니다.

---

## 6. 보안 체크리스트 (사내 환경 사용 시)

자동화는 사내 계정/시스템과 연동되니 아래는 꼭 확인하세요.

- [ ] **계정/비밀번호를 채팅창에 직접 입력하지 않기.** AI가 만든 프로그램이 켜진 후, **프로그램 GUI 안에서** 입력하세요.
- [ ] **캡쳐에 민감 정보 노출 주의.** 거래처명, 고객명, 금액, 주민번호 등이 보이면 모자이크 처리 또는 더미 데이터로 다시 캡쳐.
- [ ] **PRD 외 사내 문서를 폴더에 넣지 않기.** AI는 폴더의 모든 파일을 읽을 수 있습니다.
- [ ] **완성된 .exe를 외부 공유 시 IT/보안팀 확인.** 특히 SAP/ERP 자동화는 사내 정책 확인 필수.
- [ ] **사내 AI 사용 가이드라인** 이 있다면 사전에 확인. (사내 데이터의 외부 LLM 전송 정책 등)

---

## 7. 자주 묻는 질문

**Q. 코딩을 진짜 1도 몰라도 되나요?**
A. 네. 다만 "내가 평소 하는 업무 흐름"은 잘 알고 있어야 합니다. "1단계는 로그인하고, 2단계는 이 메뉴 클릭하고..." 정도는 본인이 머릿속에 있어야 AI가 자동화를 만들 수 있어요.

**Q. 만들어진 프로그램은 누구 거예요?**
A. 본인 것입니다. 폴더에 모든 코드가 있고, 본인이 자유롭게 수정/배포할 수 있습니다.

**Q. AI가 만든 코드가 안전한가요?**
A. 본 PRD에는 보안 체크리스트가 들어 있고, AI는 그것을 따릅니다. 다만 사내 배포 전엔 IT/보안팀의 검토를 권장합니다.

**Q. 한 번 만든 자동화를 동료에게 줄 수 있나요?**
A. 네. 빌드된 `.exe` 파일 하나만 보내주면 됩니다. 받은 사람은 Python 설치도 필요 없어요.

**Q. 자동화 대상 화면이 바뀌면 어떻게 하나요?**
A. 같은 폴더에서 CLI AI를 다시 켜고 "Step 3 화면이 바뀌었어. 새 캡쳐 줄게" 하시면 AI가 해당 부분만 수정합니다.

**Q. 동시에 여러 자동화 만들 수 있나요?**
A. 각각 별도 폴더에서 진행하시면 됩니다.

---

## 8. 막혔을 때 (Last Resort)

위 안내로도 막힌 경우:

1. **AI에게 그대로 보여주기**: 무슨 화면에서 뭐가 안 되는지 캡쳐로 찍어서 채팅창에 드래그 → "이게 안 돼" 한 마디
2. **PRD 다시 첨부**: "`AUTOMATION_BUILDER_PRD.md` 다시 읽고 처음부터 안내해줘"
3. **새 세션으로 시작**: 터미널 닫고 다시 켜기 (상태는 유지됨)
4. **사내 IT팀 문의**: 사내 환경 문제(네트워크, 권한)는 IT팀이 빠릅니다.

---

## 한 줄 요약

> **폴더 만들고 → PRD 넣고 → 그 폴더에서 CLI AI 켜고 → "PRD 읽고 시작해줘" 입력**

그 다음부터는 대화만 하시면 됩니다. 즐거운 자동화 되세요.
